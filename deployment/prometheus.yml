# Prometheus Configuration
# Scrape metrics from the Cats vs Dogs API

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "catdog-api"
    static_configs:
      - targets: ["catdog-api:8000"]
    metrics_path: "/metrics"
    scrape_interval: 10s
